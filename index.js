// This is the File that registers our javascript bundle to the Native Operating systems for iOS and Android
/**
 * @format
 */

import {AppRegistry} from 'react-native';
import App from './App';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => App);
